// Подключение пространства имен System, содержащего базовые типы данных и функции.
using System;
// Подключение пространства имен System.Collections.Generic, содержащего интерфейсы и классы для работы со списками и коллекциями. В данном коде не используется.
using System.Collections.Generic;
// Подключение пространства имен System.ComponentModel, содержащего классы для работы со свойствами компонентов и данных. В данном коде не используется.
using System.ComponentModel;
// Подключение пространства имен System.Data, содержащего классы для работы с данными. В данном коде не используется.
using System.Data;
// Подключение пространства имен System.Drawing, содержащего классы для работы с графикой. В данном коде не используется.
using System.Drawing;
// Подключение пространства имен System.Linq, содержащего классы для работы с запросами LINQ. В данном коде не используется.
using System.Linq;
// Подключение пространства имен System.Text, содержащего классы для работы со строками. В данном коде не используется.
using System.Text;
// Подключение пространства имен System.Threading.Tasks, содержащего классы для работы с асинхронными операциями. В данном коде не используется.
using System.Threading.Tasks;
// Подключение пространства имен System.Windows.Forms, содержащего классы для работы с Windows Forms.
using System.Windows.Forms;

// Объявление пространства имен проекта. Название слишком длинное и неинформативное. Рекомендуется изменить на что-то более короткое и осмысленное, например, `SalonBeauty.Forms`.
namespace курсовая_работа_3_курс__салон_красоты_
{
    // Объявление частичного класса Form7, наследуемого от класса Form. `partial` указывает на то, что определение класса разделено на несколько файлов (один генерируется дизайнером форм, другой пишется вручную).
    public partial class Form7 : Form
    {
        // Конструктор класса Form7.
        public Form7()
        {
            // Вызов метода InitializeComponent(), автоматически генерируемого дизайнером форм, для инициализации компонентов формы.
            InitializeComponent();
        }

        // Обработчик события Click для кнопки button1.
        private void button1_Click(object sender, EventArgs e)
        {
            // Закрывает текущую форму (Form7).
            Close();
        }

        // Обработчик события Click для кнопки button2.
        private void button2_Click(object sender, EventArgs e)
        {
            // Создает экземпляр формы Form8.
            Form8 Master1Form = new Form8();
            // Отображает форму Form8 в модальном режиме. Это блокирует взаимодействие с Form7, пока Form8 не будет закрыта.
            Master1Form.ShowDialog();
        }
    }
}
